import { AdjustFontSizeDirective } from './adjust-font-size.directive';

describe('AdjustFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdjustFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
