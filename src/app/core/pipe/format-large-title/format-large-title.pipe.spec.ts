import { FormatLargeTitlePipe } from "./format-large-title.pipe";

describe('FormatLargeTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLargeTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
