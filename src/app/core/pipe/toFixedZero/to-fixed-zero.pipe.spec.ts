import { ToFixedZeroPipe } from './to-fixed-zero.pipe';

describe('ToFixedZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ToFixedZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
